# Corporate Credit Grading Model

Feedforward neural network (MLPClassifier) for corporate obligor
PD estimation with Platt scaling calibration and internal grade mapping.

## Quick Start

```bash
pip install -r requirements.txt
python -m src.pipelines.train
```
